
Las clases son como moldes para poder crear más variables o elementos dentro del codigo de forma más rapida si necesidad de estar creando y llenando del mismo codigo usando la base de la POO(Programción orientada a objetos).

Las clases tienen constructores e igual que en javascript el nombre de la clase empieza con mayuscula tanto en su archivo como en el nombre de la clase al iniciar.

Las clases también tienen constructores con los que podemos trabajar para poder tener una clase con más factores.

`class IceCream{`
  `String flavor = "unknown";`
  `bool sugarFree = false;`
  `double price = 4.99;`
  `String size = "Medium";`

  `void charge(){`
    `print("The price of an Ice cream flavor $flavor size $size is $price \$");`
  `}`

`}`
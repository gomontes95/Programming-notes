String a = '2'
String b = '4'

String concatenated = a + b; // seria el resultado de combinar textos y no numeros dando 24

int c = 2
int d = 4

int result = a + b; // seria la suma dando 6

Dejo algunas operaciones 

a - b // resta
a * b // multiplicación
a / b // División
a ~/ b  // División sin decimal
a % b // Módule

a += b // Adición a lo ya existente daria 6 
a -= b // resta a lo ya existente daria 6
a * = b // multiplicación a lo ya existente daria 6

a++ // suma 1 cada vez despues que se ejecute
a--  // resta 1 cada vez despues que se ejecute

print( 'El resultado es: ${++a}'); // si queremos añadir más valores dentro de unas comillas despues de un signo $ se usan las llaves y si queremos hacer la suma antes 
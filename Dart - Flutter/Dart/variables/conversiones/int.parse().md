es una función que permite transformar un string a un int;

String toNumber = '31';
int numberConverted = int.parse(toNumber)

print(numberConverted); // Hara el print de 31 como un integer y no como un string;
